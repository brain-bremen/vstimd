# Route vstimd messages from journald to a dedicated log file.
# journald forwards to syslog automatically when rsyslog is installed.
if $programname == 'vstimd' then {
    action(type="omfile" file="/var/log/vstimd/vstimd.log")
    stop
}
